<!-- Sidebar -->
<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

  <!-- Sidebar - Brand -->
  <a class="sidebar-brand d-flex align-items-center justify-content-center" href="<%= root_path %>">
    <div class="sidebar-brand-icon rotate-n-15">
      <i class="fas fa-laugh-wink"></i>
    </div>
    <div class="sidebar-brand-text mx-3">NSCSCC <sup><%= get_current_year %></sup></div>
  </a>

  <!-- Divider -->
  <hr class="sidebar-divider my-0">

  <!-- Nav Item - Dashboard -->
  <li class="nav-item">
    <a class="nav-link" href="<%= root_path %>">
      <i class="fas fa-fw fa-table"></i>
      <span>Ranking table</span></a>
  </li>

  <!-- Divider -->
  <hr class="sidebar-divider">

  <!-- Heading -->
  <div class="sidebar-heading">
    Competition
  </div>

  <li class="nav-item <%= add_active_class(submit_files_path) %>">
    <a class="nav-link" href="<%= submit_files_path %>">
      <i class="fas fa-fw fa-cloud-upload-alt"></i>
      <span>Result upload</span></a>
  </li>

  <!-- Divider -->
  <hr class="sidebar-divider">

  <!-- Heading -->
  <div class="sidebar-heading">
    User Information
  </div>

  <li class="nav-item <%= add_active_class(user_path current_user) %>">
    <a class="nav-link" href="<%= user_path current_user %>">
      <i class="fas fa-fw fa-info-circle"></i>
      <span>User Information</span></a>
  </li>

<% if is_admin? %>
  <!-- Divider -->
  <hr class="sidebar-divider">

  <!-- Heading -->
  <div class="sidebar-heading">
    Manager
  </div>

  <!-- Nav Item - Pages Collapse Menu -->
  <%= nav_context :components, 'Scores', icon: 'random', active_link: active_link?(includes: ['base_score']) do %>
    <h6 class="collapse-header">Score setting</h6>
    <%= active_link_to 'Base score setting', admin_base_score_index_path, class: 'collapse-item' %>
  <% end %>

  <!-- Nav Item - Utilities Collapse Menu -->
  <%= nav_context :utilities, 'Files', icon: 'file-pdf', active_link: active_link?(includes: ['file_manager']) do %>
    <h6 class="collapse-header">Files setting</h6>
    <%= active_link_to 'File manage', admin_file_manager_index_path, class: 'collapse-item' %>
  <% end %>

  <li class="nav-item <%= add_active_class(users_path) %>">
    <a class="nav-link" href="<%= users_path %>">
      <i class="fas fa-fw fa-user"></i>
      <span>User manage</span></a>
  </li>
<% end %>

  <!-- Divider -->
  <hr class="sidebar-divider d-none d-md-block">

  <!-- Sidebar Toggler (Sidebar) -->
  <div class="text-center d-none d-md-inline">
    <button class="rounded-circle border-0" id="sidebarToggle"></button>
  </div>

</ul>
<!-- End of Sidebar -->
